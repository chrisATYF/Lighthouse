@{
    ViewBag.Title = "Home Page";
}

<div class="hdngImg">
    <div class="sText">
        <div class="hdngSect">
            <h1>LightHouse</h1>
            <hr class="my-4" />
            <div class="acctLinks text-center">
                <span><a href="@Url.Action("Login", "Account")" class="acctLinks">Log In</a></span>
                <span><a href="@Url.Action("Register", "Account")" class="acctLinks">Register</a></span>
            </div>
        </div>
    </div>
</div>
